# Simon Akhter — portfolio

Ascent: start at 12,000 ft, scroll down through recent work. Hover or click a project to open it on the right. Resume stays pinned with location.

```bash
npm install
npm run dev
```
